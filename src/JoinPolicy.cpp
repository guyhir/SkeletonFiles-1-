#include "../include/JoinPolicy.h"
#include "../include/Agent.h"

MandatesJoinPolicy::MandatesJoinPolicy() {}

Agent MandatesJoinPolicy:: join(vector<Agent> agentOffers)
{ int maxMandates=0;
   Agent *currentBestAgent=&agentOffers[0] ;
for (Agent a : agentOffers)
{
    if (*a.)
}
}
